name: Skye Community AI Greeting

on:
  workflow_dispatch:
    inputs:
      trigger_source:
        description: 'Source (openwrt/manual/test)'
        required: false
        default: 'manual'
        type: string
      greeting_type:
        description: 'Greeting type'
        required: false
        default: 'auto'
        type: choice
        options: [auto, morning, afternoon, evening, night]
      force_weather:
        description: 'Force weather check'
        required: false
        default: false
        type: boolean
      test_mode:
        description: 'Test mode'
        required: false
        default: false
        type: boolean
      use_test_webhook:
        description: 'Send to test webhook'
        required: false
        default: false
        type: boolean

jobs:
  send-greeting:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    
    steps:
      - name: Setup Context
        id: context
        run: |
          export TZ='Asia/Jakarta'
          
          OPENWRT_MODE=false
          if command -v opkg >/dev/null 2>&1 || [[ -f /etc/openwrt_release ]] || [[ "${{ github.event.inputs.trigger_source }}" == "openwrt" ]]; then
            OPENWRT_MODE=true
            echo "🏠 OpenWrt detected - optimized timing mode"
          fi
          
          CURRENT_DAY=$(date +%A)
          CURRENT_DATE=$(date +"%B %d, %Y")
          CURRENT_TIME=$(date +"%H:%M")
          HOUR=$(date +%H | sed 's/^0*//')
          DAY_NUM=$(date +%u)
          
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            MANUAL_TYPE="${{ github.event.inputs.greeting_type }}"
            if [[ "$MANUAL_TYPE" == "auto" ]]; then
              if [[ $HOUR -ge 5 && $HOUR -lt 12 ]]; then
                TYPE="morning"
              elif [[ $HOUR -ge 12 && $HOUR -lt 17 ]]; then
                TYPE="afternoon" 
              elif [[ $HOUR -ge 17 && $HOUR -lt 21 ]]; then
                TYPE="evening"
              else
                TYPE="night"
              fi
            else
              TYPE="$MANUAL_TYPE"
            fi
          else
            if [[ $HOUR -ge 5 && $HOUR -lt 12 ]]; then
              TYPE="morning"
            elif [[ $HOUR -ge 12 && $HOUR -lt 17 ]]; then
              TYPE="afternoon" 
            elif [[ $HOUR -ge 17 && $HOUR -lt 21 ]]; then
              TYPE="evening"
            else
              TYPE="night"
            fi
          fi
          
          if [[ $DAY_NUM -eq 6 || $DAY_NUM -eq 7 ]]; then
            CONTEXT="weekend"
          else
            CONTEXT="weekday"
          fi
          
          SPECIAL_CONTEXT=""
          case "$CURRENT_DAY" in
            "Monday") SPECIAL_CONTEXT="monday_motivation" ;;
            "Wednesday") SPECIAL_CONTEXT="hump_day" ;;
            "Friday") SPECIAL_CONTEXT="weekend_countdown" ;;
            "Sunday") SPECIAL_CONTEXT="sunday_prep" ;;
          esac
          
          echo "TYPE=$TYPE" >> $GITHUB_OUTPUT
          echo "CONTEXT=$CONTEXT" >> $GITHUB_OUTPUT
          echo "CURRENT_DAY=$CURRENT_DAY" >> $GITHUB_OUTPUT
          echo "CURRENT_DATE=$CURRENT_DATE" >> $GITHUB_OUTPUT
          echo "CURRENT_TIME=$CURRENT_TIME" >> $GITHUB_OUTPUT
          echo "SPECIAL_CONTEXT=$SPECIAL_CONTEXT" >> $GITHUB_OUTPUT
          echo "DAY_NUM=$DAY_NUM" >> $GITHUB_OUTPUT
          echo "OPENWRT_MODE=$OPENWRT_MODE" >> $GITHUB_OUTPUT
          
          echo "🎯 Context: $TYPE on $CURRENT_DAY ($SPECIAL_CONTEXT)"

      - name: Get Weather (BMKG Yogyakarta)
        id: weather
        run: |
          WEATHER_INFO=""
          echo "🌤️ Fetching BMKG weather for Yogyakarta..."
          
          BMKG_RAW=$(curl -s "https://api.bmkg.go.id/publik/prakiraan-cuaca?adm4=34.04.07.2001" --connect-timeout 8 --max-time 15 || echo "")
            
          if [[ -n "$BMKG_RAW" && "$BMKG_RAW" != *"error"* && "$BMKG_RAW" != *"Error"* ]]; then
            echo "✅ BMKG API response received"
            
            TEMP=$(echo "$BMKG_RAW" | grep -o '"t":"[^"]*"' | head -1 | cut -d'"' -f4 || echo "")
            DESC=$(echo "$BMKG_RAW" | grep -o '"weather_desc":"[^"]*"' | head -1 | cut -d'"' -f4 || echo "")
            
            if [[ -n "$TEMP" || -n "$DESC" ]]; then
              echo "🌡️ Raw: $TEMP°C | $DESC"
              
              WEATHER_CASUAL=""
              if [[ -n "$DESC" ]]; then
                DESC_LOWER=$(echo "$DESC" | tr '[:upper:]' '[:lower:]')
                
                if [[ "$DESC_LOWER" == *"hujan"* || "$DESC_LOWER" == *"rain"* ]]; then
                  WEATHER_CASUAL="hujan"
                elif [[ "$DESC_LOWER" == *"gerimis"* || "$DESC_LOWER" == *"drizzle"* ]]; then
                  WEATHER_CASUAL="gerimis"
                elif [[ "$DESC_LOWER" == *"panas"* || "$DESC_LOWER" == *"hot"* || "$DESC_LOWER" == *"terik"* ]]; then
                  WEATHER_CASUAL="panas"
                elif [[ "$DESC_LOWER" == *"dingin"* || "$DESC_LOWER" == *"cold"* ]]; then
                  WEATHER_CASUAL="dingin"
                elif [[ "$DESC_LOWER" == *"sejuk"* || "$DESC_LOWER" == *"cool"* || "$DESC_LOWER" == *"adem"* ]]; then
                  WEATHER_CASUAL="sejuk"
                elif [[ "$DESC_LOWER" == *"berawan"* || "$DESC_LOWER" == *"cloudy"* ]]; then
                  WEATHER_CASUAL="berawan"
                elif [[ "$DESC_LOWER" == *"cerah"* || "$DESC_LOWER" == *"sunny"* || "$DESC_LOWER" == *"clear"* ]]; then
                  WEATHER_CASUAL="cerah"
                else
                  WEATHER_CASUAL="enak"
                fi
              fi
              
              if [[ -n "$TEMP" && -n "$WEATHER_CASUAL" ]]; then
                WEATHER_INFO="cuaca ${WEATHER_CASUAL} ${TEMP}°c"
              elif [[ -n "$WEATHER_CASUAL" ]]; then
                WEATHER_INFO="cuaca ${WEATHER_CASUAL}"
              elif [[ -n "$TEMP" ]]; then
                WEATHER_INFO="${TEMP}°c"
              fi
              
              echo "✅ BMKG Weather: $WEATHER_INFO"
            else
              echo "⚠️ Could not extract weather data from BMKG response"
              WEATHER_INFO="cuaca enak"
            fi
          else
            echo "❌ BMKG API failed, using fallback"
            WEATHER_INFO="cuaca enak"
          fi
          
          # Define weather-appropriate emojis for later use
          WEATHER_EMOJI=""
          if [[ "$WEATHER_INFO" == *"hujan"* ]]; then
            WEATHER_EMOJI="🌧️"
          elif [[ "$WEATHER_INFO" == *"gerimis"* ]]; then
            WEATHER_EMOJI="🌦️"
          elif [[ "$WEATHER_INFO" == *"panas"* ]]; then
            WEATHER_EMOJI="🔥"
          elif [[ "$WEATHER_INFO" == *"dingin"* ]]; then
            WEATHER_EMOJI="❄️"
          elif [[ "$WEATHER_INFO" == *"sejuk"* ]]; then
            WEATHER_EMOJI="🍃"
          elif [[ "$WEATHER_INFO" == *"berawan"* ]]; then
            WEATHER_EMOJI="☁️"
          elif [[ "$WEATHER_INFO" == *"cerah"* ]]; then
            WEATHER_EMOJI="☀️"
          else
            WEATHER_EMOJI="🌤️"
          fi
          
          echo "WEATHER_INFO=$WEATHER_INFO" >> $GITHUB_OUTPUT
          echo "WEATHER_EMOJI=$WEATHER_EMOJI" >> $GITHUB_OUTPUT

      - name: Generate AI Greeting
        id: ai_generation
        run: |
          echo "🤖 Generating AI greeting with FIXED approach..."
          
          # Create MINIMAL prompt to avoid token limits
          cat > /tmp/minimal_prompt.txt << 'EOL'
          Write a 2-sentence Discord greeting:
          
          Sentence 1 (English): Start with "yoooow" or "wassuuup", mention ${{ steps.context.outputs.CURRENT_DAY }} ${{ steps.context.outputs.TYPE }}, ask a gaming question. All lowercase.
          
          Sentence 2 (Indonesian): Start with "yuuuk" or "gaskeun", include slang "anjay/wkwkwk/ygy", mention ${{ steps.weather.outputs.WEATHER_INFO }}, end with emoji. Add <@&1374047008829997157> if weekend. All lowercase.
          
          Example: "yoooow monday night squad, ready for some late gaming? gaskeun mabar valorant, cuaca sejuk perfect buat grinding wkwkwk! 🎮"
          
          Write only the 2 sentences:
          EOL
          
          # Create SIMPLIFIED Python script that WILL work
          cat > /tmp/super_simple_generator.py << 'EOL'
          import json
          import requests
          import os
          import sys
          import re
          import random
          
          def create_dynamic_ai_message():
              """Creates contextually appropriate, human-like messages"""
              day = os.environ.get('CURRENT_DAY', 'today').lower()
              time = os.environ.get('TIME_OF_DAY', 'day')
              weather = os.environ.get('WEATHER_INFO', 'cuaca enak')
              is_weekend = os.environ.get('IS_WEEKEND', 'false').lower() == 'true'
              
              # Get actual hour for more precise context
              import datetime
              jakarta_time = datetime.datetime.now()
              hour = jakarta_time.hour
              
              # More contextually appropriate starters and activities
              english_starters = ["yoooow", "wassuuup", "heyyy", "bro", "suuup"]
              
              # Context-aware questions that make sense for the time and day
              if time == 'morning':
                  if is_weekend:
                      questions = [
                          f"weekend lazy morning atau udah siap gaming {day}?",
                          f"coffee first atau straight to valorant {day}?",
                          f"bangun pagi weekend vibes atau kebangun sendiri {day}?",
                          f"productive weekend morning atau chill mode {day}?"
                      ]
                      activities = [
                          "ngopi sambil daily quest mobile legends",
                          "chill valorant unrated sambil sarapan", 
                          "roblox building sambil stretching",
                          "warm up aim training sambil ngopi"
                      ]
                  else:
                      questions = [
                          f"siap sekolah/kerja atau masih butuh coffee {day}?",
                          f"energy level udah full atau masih loading {day}?",
                          f"weekend masih jauh ya {day} morning nih?",
                          f"semangat {day} atau butuh motivasi extra?"
                      ]
                      activities = [
                          "quick valorant deathmatch sebelum berangkat",
                          "daily login mobile legends sambil sarapan",
                          "chill roblox bentar sebelum sibuk",
                          "coffee break sambil check rank"
                      ]
              
              elif time == 'afternoon':
                  if is_weekend:
                      questions = [
                          f"lunch coma atau masih semangat gaming {day}?",
                          f"productive weekend afternoon atau santai total {day}?",
                          f"panas siang gini perfect buat indoor gaming {day}?",
                          f"weekend vibes masih going strong {day}?"
                      ]
                      activities = [
                          "ranked push mobile legends sambil makan",
                          "competitive valorant session",
                          "roblox adventure sambil ngadem",
                          "team fight bareng sambil snacking"
                      ]
                  else:
                      questions = [
                          f"lunch break atau masih stuck di aktivitas {day}?",
                          f"capek seharian butuh gaming therapy {day}?",
                          f"siang bolong gini enak buat break sejenak {day}?",
                          f"weekend countdown mode udah aktif {day}?"
                      ]
                      activities = [
                          "quick mobile legends classic break time",
                          "valorant unrated sambil istirahat",
                          "chill roblox sambil charging hp",
                          "aim training sambil snack break"
                      ]
              
              elif time == 'evening':
                  questions = [
                      f"day was productive atau disaster total {day}?",
                      f"dinner time gaming atau family time dulu {day}?",
                      f"cape seharian butuh gaming relaxation {day}?",
                      f"golden hour vibes perfect buat voice chat {day}?"
                  ]
                  activities = [
                      "mabar valorant sambil dinner",
                      "mobile legends team fight bareng",
                      "roblox hangout sambil makan malam", 
                      "chill gaming session sambil ngobrol"
                  ]
              
              else:  # night
                  if hour >= 22 or hour <= 2:  # Late night (10 PM - 2 AM)
                      if is_weekend:
                          questions = [
                              f"weekend late night freedom atau udah ngantuk {day}?",
                              f"marathon gaming session atau peaceful vibes {day}?",
                              f"night owl energy atau forcing yourself {day}?",
                              f"weekend privilege jadi begadang legal {day}?"
                          ]
                          activities = [
                              "competitive valorant push rank",
                              "mobile legends night session bareng",
                              "roblox building project sambil ngobrol",
                              "late night gaming marathon"
                          ]
                      else:
                          # Weekday late night - more responsible messaging
                          questions = [
                              f"insomnia atau sengaja begadang sebentar {day}?",
                              f"besok {day} masih jauh atau udah close {day}?",
                              f"quick gaming atau udah waktunya istirahat {day}?",
                              f"night owl mode atau forcing stay awake {day}?"
                          ]
                          activities = [
                              "quick valorant unrated aja dulu",
                              "chill mobile legends classic bentar",
                              "roblox santai sambil wind down",
                              "aim training bentar sebelum bobo"
                          ]
                  else:  # Early night (6-10 PM)
                      questions = [
                          f"prime gaming time atau family time dulu {day}?",
                          f"dinner done jadi gaming time {day}?",
                          f"evening energy masih ada atau udah drop {day}?",
                          f"peaceful night gaming atau competitive mode {day}?"
                      ]
                      activities = [
                          "prime time valorant competitive",
                          "mobile legends ranked push",
                          "roblox voice chat sambil chill",
                          "evening gaming session bareng"
                      ]
              
              # Indonesian dynamic parts with proper slang
              indo_starters = ["yuuuk", "gaskeun", "gas", "skuyyyy", "ayoook"]
              slang_terms = ["anjay", "wkwkwk", "ygy", "njirrr", "sih", "dah", "bet"]
              emojis = ["🎮", "💯", "🔥", "⚡", "✨", "🎯", "💪", "🚀"]
              
              # Build contextually appropriate message
              starter = random.choice(english_starters)
              question = random.choice(questions)
              
              indo_starter = random.choice(indo_starters)
              activity = random.choice(activities)
              slang = random.choice(slang_terms)
              emoji = random.choice(emojis)
              
              # Weekend tag only for appropriate times
              tag = ""
              if is_weekend and (time in ['morning', 'afternoon', 'evening'] or (time == 'night' and hour <= 23)):
                  tag = " <@&1374047008829997157>"
              
              # Indonesian day mapping
              day_indo_map = {
                  'monday': 'senin', 'tuesday': 'selasa', 'wednesday': 'rabu',
                  'thursday': 'kamis', 'friday': 'jumat', 'saturday': 'sabtu', 'sunday': 'minggu'
              }
              day_indo = day_indo_map.get(day, day)
              
              # Create natural message
              message = f"{starter} {question} {indo_starter}{tag} {activity}, {weather} di {day_indo} gini {slang} perfect {emoji}"
              
              return message
          
          try:
              # Try Gemini API first
              with open('/tmp/minimal_prompt.txt', 'r') as f:
                  prompt = f.read().strip()
              
              api_key = os.environ.get('GEMINI_API_KEY')
              if not api_key:
                  print("❌ No API key, using dynamic generation")
                  message = create_dynamic_message()
                  with open('/tmp/ai_response.txt', 'w', encoding='utf-8') as f:
                      f.write(message)
                  print(f"✅ Dynamic generation: {message[:50]}...")
                  sys.exit(0)
              
              # Use flash-lite with VERY low token limit to avoid MAX_TOKENS
              url = f"https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-lite:generateContent?key={api_key}"
              
              payload = {
                  "contents": [{"parts": [{"text": prompt}]}],
                  "generationConfig": {
                      "temperature": 0.8,
                      "maxOutputTokens": 80,  # VERY LOW to avoid MAX_TOKENS
                      "topP": 0.9
                  }
              }
              
              print("📡 Trying Gemini with minimal tokens...")
              response = requests.post(url, json=payload, timeout=15)
              
              if response.status_code == 200:
                  data = response.json()
                  print(f"API Response: {str(data)[:200]}...")
                  
                  # Try to extract text from various possible structures
                  text_content = None
                  
                  if 'candidates' in data and data['candidates']:
                      candidate = data['candidates'][0]
                      
                      # Check finish reason
                      finish_reason = candidate.get('finishReason', '')
                      if finish_reason == 'MAX_TOKENS':
                          print("⚠️ Hit MAX_TOKENS, using dynamic generation")
                          message = create_dynamic_message()
                          with open('/tmp/ai_response.txt', 'w', encoding='utf-8') as f:
                              f.write(message)
                          print(f"✅ Dynamic fallback: {message[:50]}...")
                          sys.exit(0)
                      
                      # Try to extract text
                      if 'content' in candidate:
                          content = candidate['content']
                          if 'parts' in content and content['parts']:
                              if 'text' in content['parts'][0]:
                                  text_content = content['parts'][0]['text']
                              else:
                                  # Sometimes text is directly in parts
                                  text_content = str(content['parts'][0])
                          elif 'text' in content:
                              text_content = content['text']
                      elif 'text' in candidate:
                          text_content = candidate['text']
                  
                  if text_content and len(text_content.strip()) > 20:
                      # Clean the text
                      clean_text = text_content.strip()
                      clean_text = re.sub(r'\*\*([^*]+)\*\*', r'\1', clean_text)
                      clean_text = re.sub(r'`([^`]+)`', r'\1', clean_text)
                      clean_text = ' '.join(clean_text.split())
                      
                      # Make sure it's lowercase
                      clean_text = clean_text.lower()
                      
                      # Save the AI response
                      with open('/tmp/ai_response.txt', 'w', encoding='utf-8') as f:
                          f.write(clean_text)
                      print(f"✅ Gemini success: {clean_text[:50]}...")
                      sys.exit(0)
              
              # If we get here, Gemini failed, use dynamic generation
              print("⚠️ Gemini failed, using dynamic generation")
              message = create_dynamic_message()
              with open('/tmp/ai_response.txt', 'w', encoding='utf-8') as f:
                  f.write(message)
              print(f"✅ Dynamic generation: {message[:50]}...")
              sys.exit(0)
              
          except Exception as e:
              print(f"⚠️ Exception: {e}, using dynamic generation")
              message = create_dynamic_message()
              with open('/tmp/ai_response.txt', 'w', encoding='utf-8') as f:
                  f.write(message)
              print(f"✅ Dynamic generation: {message[:50]}...")
              sys.exit(0)
          EOL

          # Create Discord payload script
          cat > /tmp/discord_script.py << 'EOL'
          import json
          import os
          
          title = os.environ.get('TITLE', 'Skye Squad')
          desc_raw = os.environ.get('DESC', 'what\'s good everyone!')
          color = int(os.environ.get('COLOR', '16766720'))
          timestamp = os.environ.get('TIMESTAMP', '')
          thumb = os.environ.get('THUMB', '1f305')
          
          # Convert description to lowercase
          desc = desc_raw.lower()
          
          # Add signature
          desc += "\n> by server dev guanteng - <@399393175904714752>"
          
          embed = {
              "title": title,
              "description": desc,
              "color": color,
              "thumbnail": {
                  "url": f"https://twemoji.maxcdn.com/v/latest/72x72/{thumb}.png"
              },
              "footer": {
                  "text": "chill gaming vibes",
                  "icon_url": "https://files.catbox.moe/npfh4e.jpg"
              },
              "timestamp": timestamp
          }
          
          payload = {"embeds": [embed]}
          
          with open('/tmp/discord_payload.json', 'w', encoding='utf-8') as f:
              json.dump(payload, f, ensure_ascii=False)
          
          print("✅ Payload created")
          EOL

          # Execute with environment variables for dynamic generation
          echo "🧪 Executing FIXED generator..."
          CURRENT_DAY="${{ steps.context.outputs.CURRENT_DAY }}" \
          TIME_OF_DAY="${{ steps.context.outputs.TYPE }}" \
          WEATHER_INFO="${{ steps.weather.outputs.WEATHER_INFO }}" \
          IS_WEEKEND="${{ steps.context.outputs.CONTEXT == 'weekend' }}" \
          GEMINI_API_KEY="${{ secrets.GEMINI_API_KEY }}" \
          python3 /tmp/super_simple_generator.py
          
          # This should ALWAYS succeed now
          if [[ -s /tmp/ai_response.txt ]]; then
            AI_TEXT=$(cat /tmp/ai_response.txt)
            echo "🎯 Generated text: $AI_TEXT"
            
            # RELAXED validation - if we have reasonable content, use it
            if [[ ${#AI_TEXT} -gt 30 && ${#AI_TEXT} -lt 250 ]]; then
              
              # Auto-fix weekend tags if needed
              if [[ "${{ steps.context.outputs.CONTEXT }}" == "weekend" ]]; then
                HAS_TAG=$(echo "$AI_TEXT" | grep -E "<@&[0-9]+>" || echo "")
                if [[ -z "$HAS_TAG" ]]; then
                  echo "⚠️ Adding weekend tag..."
                  # Find good insertion point after first sentence
                  if [[ "$AI_TEXT" == *"?"* ]]; then
                    AI_TEXT=$(echo "$AI_TEXT" | sed 's/? /? <@&1374047008829997157> /')
                  else
                    AI_TEXT="$AI_TEXT <@&1374047008829997157>"
                  fi
                fi
              else
                # Remove tags for weekdays
                AI_TEXT=$(echo "$AI_TEXT" | sed 's/<@&[0-9]*>//g')
              fi
              
              # Ensure proper length
              if [[ ${#AI_TEXT} -gt 200 ]]; then
                AI_TEXT="${AI_TEXT:0:190}..."
              fi
              
              DESC_RAW="$AI_TEXT"
              USE_AI=true
              echo "✅ AI SUCCESS: ${#AI_TEXT} chars"
            else
              echo "❌ Generated text invalid length: ${#AI_TEXT}"
              USE_AI=false
            fi
          else
            echo "❌ No AI response file generated"
            USE_AI=false
          fi
          
          # ONLY use traditional fallback if everything fails (this should be rare now)
          if [[ "$USE_AI" != "true" ]]; then
            echo "🎲 Using traditional fallback (this shouldn't happen often)..."
            
            WEATHER_MENTION="${{ steps.weather.outputs.WEATHER_INFO }}"
            WEATHER_EMOJI="${{ steps.weather.outputs.WEATHER_EMOJI }}"
            DAY_LOWER=$(echo "${{ steps.context.outputs.CURRENT_DAY }}" | tr '[:upper:]' '[:lower:]')
            
            case "${{ steps.context.outputs.TYPE }}" in
              "morning")
                if [[ "${{ steps.context.outputs.CONTEXT }}" == "weekend" ]]; then
                  DESC_RAW="yoooow ${DAY_LOWER} morning crew, weekend vibes hitting different? gaskeun <@&1374047008829997157> warm up aim sambil ngopi, ${WEATHER_MENTION} di ${DAY_LOWER} gini anjay bgt buat grinding! ${WEATHER_EMOJI}"
                else
                  DESC_RAW="heyyy ${DAY_LOWER} morning gang, ready for the day or still zombie mode? semangat ${DAY_LOWER} ini wkwkwk, ${WEATHER_MENTION} pas bgt buat fresh start ygy! ${WEATHER_EMOJI}"
                fi
                ;;
              "afternoon")
                if [[ "${{ steps.context.outputs.CONTEXT }}" == "weekend" ]]; then
                  DESC_RAW="wassuuup ${DAY_LOWER} afternoon squad, lunch coma or still grinding? yuuuk <@&1374047008829997157> quick match valorant, ${WEATHER_MENTION} di ${DAY_LOWER} gini bjirrr enak bgt dah! ${WEATHER_EMOJI}"
                else
                  DESC_RAW="bro ${DAY_LOWER} siang, lunch break atau masih grind? istirahat dulu kali yak, ${WEATHER_MENTION} di ${DAY_LOWER} gini anjay enak buat refreshing! ${WEATHER_EMOJI}"
                fi
                ;;
              "evening")
                if [[ "${{ steps.context.outputs.CONTEXT }}" == "weekend" ]]; then
                  DESC_RAW="suuup ${DAY_LOWER} evening legends, weekend still going strong? skuyyyy <@&1374047008829997157> mabar valorant nih, ${WEATHER_MENTION} di ${DAY_LOWER} sore gini wkwkwk perfect bet! ${WEATHER_EMOJI}"
                else
                  DESC_RAW="yoooow ${DAY_LOWER} evening, school/work done for today? gas santai dulu abis capek seharian, ${WEATHER_MENTION} di ${DAY_LOWER} gini ygy pas buat relaxing! ${WEATHER_EMOJI}"
                fi
                ;;
              "night")
                if [[ "${{ steps.context.outputs.CONTEXT }}" == "weekend" ]]; then
                  DESC_RAW="wassuuup ${DAY_LOWER} night gang, still awake and gaming? yuuuk <@&1374047008829997157> push rank valorant, ${WEATHER_MENTION} malem ${DAY_LOWER} gini anjay sih buat naikin elo! ${WEATHER_EMOJI}"
                else
                  if [[ "${{ steps.context.outputs.CURRENT_DAY }}" == "Sunday" ]]; then
                    DESC_RAW="heyyy sunday night crew, ready for monday or denial mode? jgn begadang y besok udh senin wkwkwk, ${WEATHER_MENTION} gini enak buat bobo ygy! ${WEATHER_EMOJI}"
                  else
                    DESC_RAW="bro ${DAY_LOWER} malam, grinding atau mau bobo soon? jgn begadang mulu deh besok ${DAY_LOWER}, ${WEATHER_MENTION} gini bjirrr enak bat buat istirahat! ${WEATHER_EMOJI}"
                  fi
                fi
                ;;
            esac
            
            echo "🎯 Traditional fallback: ${DESC_RAW}"
          fi
          
          # Set title and thumbnail based on time and weather
          if [[ "${{ steps.context.outputs.TYPE }}" == "morning" ]]; then
            if [[ "${{ steps.weather.outputs.WEATHER_INFO }}" == *"hujan"* ]]; then
              TITLE="☔ Rainy Morning Vibes"
              THUMB="2614"
            else
              TITLE="☀️ Morning Skye Squad"
              THUMB="2600"
            fi
          elif [[ "${{ steps.context.outputs.TYPE }}" == "afternoon" ]]; then
            if [[ "${{ steps.weather.outputs.WEATHER_INFO }}" == *"hujan"* ]]; then
              TITLE="🌧️ Rainy Afternoon Chill"
              THUMB="1f327"
            else
              TITLE="🍃 Afternoon Chill Mode"
              THUMB="1f343"
            fi
          elif [[ "${{ steps.context.outputs.TYPE }}" == "evening" ]]; then
            if [[ "${{ steps.weather.outputs.WEATHER_INFO }}" == *"hujan"* ]]; then
              TITLE="🌧️ Rainy Evening Chill"
              THUMB="1f327"
            else
              TITLE="🌇 Evening Chill Squad"
              THUMB="1f307"
            fi
          else
            if [[ "${{ steps.weather.outputs.WEATHER_INFO }}" == *"hujan"* ]]; then
              TITLE="🌧️ Rainy Night Gaming"
              THUMB="1f327"
            else
              TITLE="🦉 Late Night Squad"
              THUMB="1f989"
            fi
          fi
          
          # Output values for next steps
          echo "TITLE=$TITLE" >> $GITHUB_OUTPUT
          echo "DESC_RAW=$DESC_RAW" >> $GITHUB_OUTPUT
          echo "USE_AI=$USE_AI" >> $GITHUB_OUTPUT
          echo "THUMB=$THUMB" >> $GITHUB_OUTPUT

      - name: Send Discord Message
        if: ${{ github.event.inputs.test_mode != 'true' }}
        run: |
          echo "📤 Sending Discord message..."
          
          case "${{ steps.context.outputs.TYPE }}" in
            "morning") COLOR=16766720 ;;
            "afternoon") COLOR=16763904 ;;
            "evening") COLOR=16744448 ;;
            "night") COLOR=8947848 ;;
          esac
          
          TITLE="${{ steps.ai_generation.outputs.TITLE }}"
          DESC="${{ steps.ai_generation.outputs.DESC_RAW }}"
          THUMB="${{ steps.ai_generation.outputs.THUMB }}"
          TIMESTAMP=$(TZ='Asia/Jakarta' date -u +%Y-%m-%dT%H:%M:%S.000Z)
          
          # Select webhook based on test mode
          if [[ "${{ github.event.inputs.use_test_webhook }}" == "true" ]]; then
            WEBHOOK_URL="${{ secrets.DISCORD_WEBHOOK_TEST }}"
            echo "Using TEST webhook"
          else
            WEBHOOK_URL="${{ secrets.DISCORD_WEBHOOK_URL }}"
            echo "Using PRODUCTION webhook"
          fi
          
          # Run Python script with environment variables
          TITLE="$TITLE" DESC="$DESC" COLOR="$COLOR" TIMESTAMP="$TIMESTAMP" THUMB="$THUMB" python3 /tmp/discord_script.py
          
          curl -f -X POST "$WEBHOOK_URL" \
               -H "Content-Type: application/json" \
               --data-binary @/tmp/discord_payload.json
          
          echo "✅ Message sent!"
          echo "🤖 AI Used: ${{ steps.ai_generation.outputs.USE_AI }}"

      - name: Test Mode Output
        if: ${{ github.event.inputs.test_mode == 'true' }}
        run: |
          echo "🧪 TEST MODE - Message preview:"
          echo "================================"
          echo "Title: ${{ steps.ai_generation.outputs.TITLE }}"
          echo "Description: ${{ steps.ai_generation.outputs.DESC_RAW }}"
          echo "AI Used: ${{ steps.ai_generation.outputs.USE_AI }}"
          echo "Weather: ${{ steps.weather.outputs.WEATHER_INFO }}"
          echo "Context: ${{ steps.context.outputs.TYPE }} (${{ steps.context.outputs.SPECIAL_CONTEXT }})"
          echo "OpenWrt Mode: ${{ steps.context.outputs.OPENWRT_MODE }}"
          if [[ "${{ github.event.inputs.use_test_webhook }}" == "true" ]]; then
            echo "Webhook: TEST"
          else
            echo "Webhook: PRODUCTION"
          fi
          echo "================================"
          echo "🧪 Test complete - no message sent"

      - name: Send Random Sticker
        if: ${{ github.event.inputs.test_mode != 'true' }}
        run: |
          echo "🎲 Sending random sticker..."
          
          if [[ "${{ steps.context.outputs.OPENWRT_MODE }}" == "true" ]]; then
            echo "🏠 OpenWrt mode - sending sticker immediately"
            sleep 1
          else
            echo "⏰ Standard delay for sticker..."
            sleep 3
          fi
          
          # Select webhook based on test mode
          if [[ "${{ github.event.inputs.use_test_webhook }}" == "true" ]]; then
            WEBHOOK_URL="${{ secrets.DISCORD_WEBHOOK_TEST }}"
          else
            WEBHOOK_URL="${{ secrets.DISCORD_WEBHOOK_URL }}"
          fi
          
          # Simple sticker selection
          if [[ "${{ steps.context.outputs.TYPE }}" == "morning" ]]; then
            STICKER="https://media.discordapp.net/stickers/1404543938815197306.png"
          elif [[ "${{ steps.context.outputs.TYPE }}" == "afternoon" ]]; then
            STICKER="https://media.discordapp.net/stickers/1405487683526201404.png"
          elif [[ "${{ steps.context.outputs.TYPE }}" == "evening" ]]; then
            STICKER="https://media.discordapp.net/stickers/1405914196922597549.png"
          else
            STICKER="https://media.discordapp.net/stickers/1406668584335184003.png"
          fi
          
          curl -f -X POST "$WEBHOOK_URL" \
               -H "Content-Type: application/json" \
               -d "{\"content\":\"$STICKER\"}"
          
          echo "🎉 Sticker sent!"

      - name: Execution Summary
        run: |
          echo "📊 EXECUTION SUMMARY"
          echo "================================"
          echo "🕐 Time: ${{ steps.context.outputs.CURRENT_TIME }} WIB (${{ steps.context.outputs.CURRENT_DAY }})"
          echo "🎯 Type: ${{ steps.context.outputs.TYPE }} (${{ steps.context.outputs.CONTEXT }})"
          echo "🌤️ Weather: ${{ steps.weather.outputs.WEATHER_INFO }}"
          echo "🤖 AI Success: ${{ steps.ai_generation.outputs.USE_AI }}"
          echo "🏠 OpenWrt Mode: ${{ steps.context.outputs.OPENWRT_MODE }}"
          echo "🧪 Test Mode: ${{ github.event.inputs.test_mode }}"
          if [[ "${{ github.event.inputs.use_test_webhook }}" == "true" ]]; then
            echo "🔄 Using TEST webhook"
          else
            echo "🔄 Using PRODUCTION webhook"
          fi
          echo "================================"
          echo "✅ Skye community greeting complete!"